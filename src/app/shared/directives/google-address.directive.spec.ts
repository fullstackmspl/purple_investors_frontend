import { GoogleAddressDirective } from './google-address.directive';

describe('GoogleAddressDirective', () => {
  it('should create an instance', () => {
    const directive = new GoogleAddressDirective();
    expect(directive).toBeTruthy();
  });
});
